<%- include('../partials/header') %>

  <div class="container-fluid">
    <div class="row">
      <!-- Sidebar Modern (Biru) -->
      <%- include('../partials/sidebar-admin') %>

        <!-- Main Content -->
        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 py-4">
          <div class="d-flex justify-content-between align-items-center pt-3 pb-2 mb-3 border-bottom">
            <h1 class="h2">Surat Masuk</h1>
            <a href="/admin/surat-masuk/tambah" class="btn btn-success">+ Tambah</a>
          </div>

          <%- include('../partials/messages') %>

            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th>#</th>
                    <th>No Surat</th>
                    <th>Tgl Surat</th>
                    <th>Tgl Terima</th>
                    <th>Pengirim</th>
                    <th>Perihal</th>
                    <th>Aksi</th>
                  </tr>
                </thead>
                <tbody>
                  <% if (surat.length===0) { %>
                    <tr>
                      <td colspan="7" class="text-center">Belum ada data.</td>
                    </tr>
                    <% } else { %>
                      <% surat.forEach((s, i)=> { %>
                        <tr>
                          <td>
                            <%= i + 1 %>
                          </td>
                          <td>
                            <%= s.nomor_surat %>
                          </td> <!-- ✅ Pastikan kolom nama benar -->
                          <td>
                            <%= formatDate(s.tanggal_surat) %>
                          </td>
                          <td>
                            <%= formatDate(s.tgl_terima) %>
                          </td>
                          <td>
                            <%= s.pengirim %>
                          </td>
                          <td>
                            <%= s.perihal %>
                          </td>
                          <td>
                            <a href="/admin/surat-masuk/edit/<%= s.id %>" class="btn btn-sm btn-warning">Edit</a>
                            <a href="/admin/surat-masuk/hapus/<%= s.id %>" class="btn btn-sm btn-danger"
                              onclick="return confirm('Yakin hapus?')">Hapus</a>
                          </td>
                        </tr>
                        <% }); %>
                          <% } %>
                </tbody>
              </table>
            </div>
        </main>
    </div>
  </div>

  <%- include('../partials/footer') %>